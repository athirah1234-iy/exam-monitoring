# ===== Core Dependencies ===== #
firebase-admin==6.4.0
python-telegram-bot==20.3  # Upgraded to stable v20.x (v13.x is deprecated)
opencv-python-headless==4.9.0.80  # 🚨 Use HEADLESS (no GUI needed on Render!)

# ===== YOLOv8 and PyTorch ===== #
ultralytics==8.1.25
torch==2.1.2+cu118  # 🚀 CUDA-enabled for GPU (remove "+cu118" if CPU-only)
torchvision==0.16.2+cu118
torchaudio==2.1.2
numpy==1.26.4  # Explicitly pinned (avoid conflicts with OpenCV/PyTorch)

# ===== Firebase/Cloud ===== #
google-cloud-storage==2.13.0
google-auth==2.28.1

# ===== Utilities ===== #
tqdm==4.66.1
requests==2.31.0
pyyaml==6.0.1
mediapipe==0.10.9  # Added for hand detection
